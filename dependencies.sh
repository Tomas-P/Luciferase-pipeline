#!/bin/bash
# this file is for installing the dependencies on linux
# it does the same thing as the one for windows
pip install numpy
pip install pandas
pip install matplotlib
#numpy is a dependency of pandas and therefore must be installed first
# This is the same installation order I used, albiet on a windows computer.